{{ if and (not .lite) (eq .chezmoi.os "darwin") -}}
#!/bin/bash
set -euo pipefail

# ## Workaround for fuzzy-find alt+c: https://github.com/junegunn/fzf/issues/164
# bindkey "ç" fzf-cd-widget

{{ includeTemplate "homebrew-env.sh" . }}
if brew doctor; then
    echo "Homebrew checks passed"
else
    echo "WARNING: Homebrew reported issues. Review the output above." >&2
fi
{{ end -}}
